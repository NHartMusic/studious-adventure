import styled from 'styled-components'
import {MainTitle} from '../../UI/Typography'
import {randomColor1} from '../../UI/ColorGenerator'

export const ConnectTitle = styled(MainTitle)`
    color: ${randomColor1};
`
